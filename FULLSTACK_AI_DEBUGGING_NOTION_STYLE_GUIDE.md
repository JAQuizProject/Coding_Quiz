# Notion 화면 공유 스타일 가이드

대상 페이지: `FULLSTACK_AI_DEBUGGING_NOTION_SCREEN.md`

목표는 Notion을 문서처럼 읽는 것이 아니라, **한 화면에 메시지 하나가 보이는 스크롤형 발표 화면**으로 사용하는 것이다.

---

## 1. 기본 설정

| 설정 | 권장값 |
| --- | --- |
| 페이지 폭 | Full width |
| 글꼴 | Default |
| Small text | 사용하지 않음 |
| 브라우저 확대 | 110~125% |
| 페이지 Cover | 사용하지 않음 |
| 페이지 Icon | Notion 기본 아이콘 중 `Search` 또는 `Monitor` 계열 |
| 사이드바 | 발표 시작 전에 닫기 |
| 페이지 상태 | 편집 완료 후 Lock page |
| 배경 | 기본 흰색 |

Cover를 사용하지 않는 이유는 첫 화면에서 제목과 핵심 문장이 바로 보여야 하기 때문이다.

---

## 2. 색상 규칙

Notion 색상은 의미를 구분할 때만 사용한다.

| 색상 | 의미 | 사용할 블록 |
| --- | --- | --- |
| 기본 흰색 | 일반 설명 | 본문, 표 |
| Gray background | 실행 흐름과 코드 | 코드 블록, 데모 경로 |
| Blue background | 핵심 개념 | MCP 정의, 발표 핵심 문장 |
| Yellow background | 사람의 결정과 주의 | API 호환성, 권한 경계 |
| Green background | 검증 완료 | 수정 후 결과, 완료 조건 |
| Red background | 실제 실패만 | 재현된 오류, 금지 작업 |

한 화면에 배경색 Callout은 최대 두 개만 둔다. 모든 블록에 색을 입히지 않는다.

---

## 3. 타이포그래피와 밀도

- 각 화면은 `번호 Eyebrow → H1 → 핵심 블록 1개 → 한 줄 결론` 순서로 구성한다.
- `01. PROBLEM` 같은 번호 제목은 Gray text로 바꾼다.
- 한국어 핵심 문장은 H1 한 줄 또는 두 줄 이내로 유지한다.
- 본문은 한 문단에 세 줄을 넘기지 않는다.
- 표는 한 화면에 하나만 사용한다.
- 코드 블록은 7줄 이내를 기본으로 한다.
- 긴 프롬프트와 출처 설명은 Toggle로 접는다.
- 화면에 보이는 링크는 세 개 이내로 제한한다.

---

## 4. 첫 화면 스타일

```text
┌──────────────────────────────────────────────────────────────┐
│ 화면에서 API까지                                             │
│ Claude·Codex와 Browser MCP로 풀스택 버그 추적하기             │
│                                                              │
│ [Blue Callout]                                               │
│ 코드만 읽고 추측하는 AI에서, 실제 화면과 Network를           │
│ 확인하고 검증하는 AI로.                                      │
│                                                              │
│ 25 MIN     FRONTEND + BACKEND     LIVE DEBUGGING             │
└──────────────────────────────────────────────────────────────┘
```

적용 방법:

1. 페이지 제목은 그대로 둔다.
2. 부제는 H2로 둔다.
3. 첫 인용문을 Blue background Callout으로 변환한다.
4. `25 MIN` 줄은 Gray text로 바꾸고 가운데 정렬 대신 왼쪽 정렬을 유지한다.
5. 첫 화면에는 목차나 출처를 넣지 않는다.

---

## 5. 화면별 블록 배치

### 화면 01 · Problem

```text
01. PROBLEM
"제출했는데 결과 화면이 이상해요"

[Blue Callout: 사용자는 하나의 문제로 경험한다]

[화면] [브라우저] [API] [서버]

[한 줄 결론]
```

- 4열 표는 전체 폭을 사용한다.
- 마지막 결론만 Bold 처리한다.
- 오류 색상은 사용하지 않는다. 아직 원인이 확인되지 않았기 때문이다.

### 화면 02 · Shift

```text
02. SHIFT
AI에게 실행 환경을 보여준다

┌ BEFORE ─────────────┐  ┌ AFTER ───────────────────┐
│ 저장소              │  │ 사용자 조작              │
│ → 정적 추정         │  │ → DOM/Console/Network    │
└─────────────────────┘  │ → 양쪽 코드 → 재검증     │
                         └───────────────────────────┘

[Blue Callout: MCP 한 줄 정의]
```

- Before와 After 코드 블록을 2열로 배치한다.
- Before는 Gray background, After는 Blue background를 사용한다.
- 두 열의 너비는 `40 : 60`으로 둔다.

### 화면 03 · Why Now

- 공식 흐름 표를 전체 폭으로 둔다.
- 출처 링크 세 개는 한 줄에 둔다.
- CyberAgent 숫자만 Bold 처리한다.
- 영상은 이 화면에서 새 탭으로 연다.
- 세부 설명은 `근거 상세` Toggle로 접는다.

### 화면 04 · Live Demo

```text
04. LIVE DEMO
API는 성공했는데 화면은 실패한다

[Red Callout] 실제 증상
POST /quiz/submit = 200
결과 화면 = 잘못 표시

[Gray Code] 사용자 흐름

[Toggle] AI 조사 프롬프트
```

- 이 화면에서만 Red background Callout을 사용한다.
- 프롬프트는 Toggle 안에 넣어 시작할 때는 닫아 둔다.
- 데모 시작 직전에 Toggle을 열고 프롬프트를 보여준다.

### 화면 05 · Evidence

- 근거 표를 전체 폭으로 둔다.
- `확인된 사실`은 Blue text, `추정`은 Yellow text로 구분한다.
- 다섯 줄 조사 결과 형식은 Gray code block으로 둔다.
- AI의 긴 원문 응답은 Notion에 붙이지 않는다.

### 화면 06 · Verify

```text
06. VERIFY
같은 흐름으로 증명한다

┌ 수정 전 ────────────┐  ┌ 수정 후 ────────────────┐
│ API 200             │  │ API 계약 일치            │
│ 화면 실패           │  │ 화면·오답 목록 정상      │
└─────────────────────┘  └───────────────────────────┘

[Green Callout: 완료 기준]
```

- 수정 전과 수정 후를 2열로 배치한다.
- 수정 전은 Gray background, 수정 후는 Green background를 사용한다.
- 완료 기준 인용문을 Green Callout으로 변환한다.

### 화면 07 · Apply

- `먼저 적용`과 `제한`을 2열로 배치한다.
- 왼쪽은 Blue background Callout, 오른쪽은 Yellow background Callout으로 만든다.
- 측정 지표 표는 두 Callout 아래에 전체 폭으로 둔다.

### 화면 08 · Takeaway

- 중앙 정렬 대신 왼쪽 정렬을 유지한다.
- 실행 흐름 코드 블록을 전체 폭으로 둔다.
- 마지막 인용문을 Blue Callout으로 만든다.
- `REPRODUCE · TRACE · DECIDE · VERIFY`는 Gray text로 표시한다.
- 이 화면에는 출처, 다음 단계, Q&A 목록을 추가하지 않는다.

---

## 6. Notion에서 수동으로 적용할 순서

Markdown 가져오기 후 10분 안에 다음만 손본다.

1. 페이지를 Full width로 변경한다.
2. 첫 인용문을 Blue Callout으로 변경한다.
3. 각 `01~08` 제목을 Gray text로 변경한다.
4. 화면 02의 Before/After를 2열로 배치한다.
5. 화면 04의 실제 증상을 Red Callout으로 변경한다.
6. 화면 04의 프롬프트를 Toggle 안으로 이동한다.
7. 화면 06의 수정 전/후를 2열로 배치한다.
8. 화면 06의 완료 기준을 Green Callout으로 변경한다.
9. 화면 07의 적용/제한을 2열 Callout으로 변경한다.
10. 페이지를 Lock page로 전환한다.

---

## 7. 화면 공유 방법

### 발표 시작 전

- Notion 사이드바를 닫는다.
- 브라우저 탭에는 발표 Notion, 공식 영상, 로컬 앱만 남긴다.
- 개인 즐겨찾기 모음을 숨긴다.
- Windows 알림과 메신저 알림을 끈다.
- 발표 페이지를 잠가 실수로 블록을 이동하지 않게 한다.

### 발표 중

- 전체 문서를 빠르게 스크롤하지 않는다.
- 한 화면을 설명한 뒤 다음 구분선까지 한 번만 스크롤한다.
- 커서로 문장을 계속 따라가지 않는다.
- 표에서는 현재 설명하는 열 하나만 짚는다.
- 데모 전환 시 Notion을 중간 위치에 방치하지 않는다.

### 권장 화면 전환

```text
Notion 01~04
  → 테스트 Chrome
  → Claude/Codex
  → VS Code
  → 테스트 Chrome 재검증
  → Notion 06
  → Notion 08 결론
```

화면 05는 AI 조사 결과가 길 때 정리 형식을 설명하는 용도로 사용한다. 시간이 부족하면 건너뛸 수 있다.

---

## 8. 피해야 할 화면 스타일

- 긴 문단을 그대로 읽는 문서형 화면
- 한 화면에 표 두 개 이상 배치
- 모든 문장에 색상과 Bold 적용
- 제품 로고를 크게 나열한 홍보형 화면
- Notion Cover가 첫 화면 절반을 차지하는 구성
- 코드 전체 파일을 Notion에 붙여 넣는 구성
- AI 채팅 전체 로그를 스크롤하는 구성
- 발표자 메모와 청중 자료를 같은 페이지에 두는 구성

화면 공유 페이지는 설명서가 아니다. 상세 내용은 `FULLSTACK_AI_DEBUGGING_NOTION.md`에서 제공하고, 화면 공유 페이지에는 발표 순간에 필요한 메시지만 남긴다.

