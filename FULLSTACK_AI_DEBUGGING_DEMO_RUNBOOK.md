# 풀스택 AI 디버깅 발표자 런북

이 문서는 청중에게 공유하지 않는 발표자 전용 자료다. 화면 공유에는 `FULLSTACK_AI_DEBUGGING_NOTION_SCREEN.md`를 사용하고, 발표 후 상세 자료로 `FULLSTACK_AI_DEBUGGING_NOTION.md`를 공유한다.

MCP 설치와 프로젝트 최초 실행은 `FULLSTACK_AI_DEBUGGING_MCP_SETUP.md`를 먼저 따른다.

---

## 1. 운영 원칙

1. Notion은 이야기의 기준점으로 사용한다.
2. 라이브 실습에서는 Claude와 Codex 중 하나만 사용한다.
3. 브라우저에서 먼저 문제를 보여주고, 원인 설명은 나중에 한다.
4. AI에게 바로 수정시키지 않고 조사 결과를 먼저 받는다.
5. 코드보다 화면과 Network 증거를 먼저 보여준다.
6. 실습은 PR이 아니라 수정 전·후 사용자 흐름 비교로 끝낸다.

발표 권장 시간은 25분이다.

| 구간 | 시간 | 화면 |
| --- | ---: | --- |
| 주제와 문제 설명 | 0:00~6:00 | Notion |
| 최근 사례 | 6:00~9:00 | Notion + 공식 영상 20초 |
| 문제 재현 | 9:00~12:00 | 테스트용 Chrome |
| AI 조사 | 12:00~17:00 | AI 터미널/앱 + Chrome |
| 코드 확인과 수정 | 17:00~21:00 | VS Code + AI 터미널 |
| 브라우저 재검증 | 21:00~23:00 | 테스트용 Chrome |
| 결론 | 23:00~25:00 | Notion |

---

## 2. 화면 공유 구성

발표용 Windows 가상 데스크톱을 세 개 사용한다.

| 데스크톱 | 준비할 화면 | 용도 |
| --- | --- | --- |
| 1 | Notion 발표 페이지 | 설명, 사례, 결론 |
| 2 | 테스트 전용 Chrome | 문제 재현, Network 확인, 재검증 |
| 3 | VS Code + Claude/Codex | 조사 과정, 코드 근거, 수정 |

권장 방식:

- Zoom 또는 Teams에서 발표용 모니터 전체를 공유한다.
- Windows 알림과 메신저 알림을 모두 끈다.
- 개인용 Chrome이 아닌 발표 전용 프로필을 사용한다.
- Notion과 VS Code의 글자 크기를 최소 18px 수준으로 맞춘다.
- DevTools를 브라우저 오른쪽에 좁게 붙이지 말고 별도 창으로 연다.
- 바탕화면, `.env`, 토큰, 개인 북마크가 보이지 않게 한다.

라이브 중 화면 이동 순서:

```text
Notion
  -> 공식 영상 탭
  -> 테스트용 Chrome
  -> AI 터미널/앱
  -> VS Code
  -> 테스트용 Chrome
  -> Notion
```

Notion으로 돌아올 때마다 다음 섹션의 제목부터 보여준다. 긴 문서를 빠르게 스크롤하지 않는다.

---

## 3. Notion 가져오기와 공유 설정

1. Notion에서 새 페이지를 만든다.
2. `Import`에서 `Markdown & CSV`를 선택한다.
3. `FULLSTACK_AI_DEBUGGING_NOTION_SCREEN.md`를 가져온다.
4. 페이지 제목을 `화면에서 API까지`로 둔다.
5. 페이지를 `Full width`로 설정한다.
6. 첫 번째 구분선 아래에 Table of contents 블록을 추가한다.
7. 발표자 메모는 이 페이지에 추가하지 않는다.
8. `FULLSTACK_AI_DEBUGGING_NOTION.md`는 하위 페이지로 별도 가져와 발표 후 참고 자료로 공유한다.
9. 회사 내부 공유라면 웹 전체 공개 대신 워크스페이스 멤버에게만 공유한다.

Notion에서 손볼 블록:

- 첫 인용문은 Callout으로 변환한다.
- `Before`와 `After` 코드 블록은 2열 레이아웃으로 배치한다.
- 출처 링크는 Bookmark가 아니라 텍스트 링크로 유지한다.
- 긴 실습 프롬프트는 Toggle 안에 넣는다.
- `참고 자료`는 마지막 섹션에만 둔다.

발표 화면에서는 Notion 사이드바를 닫고 브라우저 확대를 110~125%로 맞춘다.

---

## 4. 실습용 문제 준비

### 증상

```text
POST /quiz/submit -> 200 OK
결과 화면 -> 점수 또는 오답 목록이 잘못 표시됨
```

### Fault Injection 기준

- 발표용 브랜치에서만 문제를 만든다.
- 프론트가 읽는 응답 필드와 백엔드 응답 모델을 불일치시킨다.
- 서버 자체는 `200` 응답을 반환하도록 한다.
- Console 오류가 있거나 화면 데이터가 비는 증상을 만든다.
- 수정은 1~2개 파일로 끝나게 한다.
- 실습 전 정답 diff를 별도 patch로 보관한다.

### 조사 대상 파일

```text
frontend/app/quiz/page.js
frontend/api/quiz.js
frontend/app/result/page.js
app/modules/quiz/router.py
app/modules/quiz/schemas.py
app/modules/quiz/service.py
```

문제의 정확한 원인은 발표 시작 전에 청중용 Notion 화면에 노출하지 않는다.

---

## 5. 발표 전 체크리스트

### T-30분

- [ ] 발표용 브랜치로 이동
- [ ] 백엔드 테스트 통과 확인
- [ ] 프론트 lint와 build 통과 확인
- [ ] 백엔드 `8000` 포트 실행
- [ ] 프론트 `3000` 포트 실행
- [ ] 테스트 계정 로그인 확인
- [ ] 실습용 문제 재현 확인
- [ ] Chrome DevTools MCP 연결 확인
- [ ] AI가 저장소를 읽을 수 있는지 확인
- [ ] 개인 브라우저와 테스트 브라우저 분리
- [ ] 알림과 메신저 종료

검증 명령:

```powershell
& '.venv\Scripts\python.exe' -m pytest -q
& '.venv\Scripts\ruff.exe' check .
Set-Location frontend
npm run lint
npm run build
```

실행 명령:

```powershell
# 터미널 1: 저장소 루트
& '.venv\Scripts\uvicorn.exe' main:app --reload --port 8000

# 터미널 2: frontend
npm run dev
```

Chrome DevTools MCP 연결 확인용 요청:

```text
Chrome DevTools MCP로 http://localhost:3000을 열고
현재 페이지 제목과 Console 오류 개수만 알려주세요.
코드는 수정하지 마세요.
```

### T-10분

- [ ] Notion을 발표 시작 위치에 고정
- [ ] 공식 영상을 사용할 시점에 맞춰 일시정지
- [ ] 테스트 브라우저 localStorage 초기화 여부 확인
- [ ] Network 기록 보존 설정 확인
- [ ] VS Code에서 관련 파일을 탭으로 미리 열기
- [ ] AI 조사 프롬프트를 클립보드 또는 메모장에 준비
- [ ] 백업 영상, 스크린샷, HAR, 조사 결과 Markdown 준비

---

## 6. 화면별 진행표

### 화면 1: Notion, 제목

보여줄 내용:

```text
화면에서 API까지
Claude·Codex와 Chrome DevTools MCP로 풀스택 버그 추적하기
```

발표 멘트:

> 오늘은 AI가 프론트와 백엔드 코드를 얼마나 많이 만드는지 이야기하지 않겠습니다. 사용자가 겪은 문제를 실제 브라우저에서 재현하고, 화면부터 API까지 근거를 연결하는 방법을 보겠습니다.

다음 화면으로 넘어갈 조건:

- 청중이 이번 발표가 코드 생성 발표가 아니라 디버깅 발표라는 점을 이해했다.

### 화면 2: Notion, 왜 오래 걸리는가

발표 멘트:

> 사용자는 "제출했는데 결과가 이상하다"고 말합니다. 프론트는 화면과 Network를 보고, 백엔드는 API와 로그를 다시 봅니다. 오늘 줄이려는 것은 코딩 시간이 아니라 이 계층 사이를 오가는 시간입니다.

`확인할 곳 / 가능한 원인` 표를 보여준다.

### 화면 3: Notion, 코드만 보는 AI와 실행 결과를 보는 AI

발표 멘트:

> 저장소만 읽은 AI는 어떤 코드가 의심스러운지는 말할 수 있지만, 사용자가 실제로 무엇을 눌렀고 어떤 HTTP 응답을 받았는지는 모릅니다. Chrome DevTools MCP가 그 실행 정보를 연결합니다.

MCP 정의는 30초를 넘기지 않는다.

### 화면 4: 공식 사례와 영상

공식 영상은 20~30초만 재생한다.

- [Chrome DevTools for agents 공식 영상](https://www.youtube.com/watch?v=1AD81ZselPk)

영상 직후 멘트:

> 핵심은 브라우저 자동 클릭이 아닙니다. AI가 화면 상태, Console, Network를 코드와 함께 볼 수 있게 됐다는 점입니다.

### 화면 5: 테스트용 Chrome, 문제 재현

직접 수행:

1. 로그인
2. 퀴즈 화면 진입
3. 답안 입력
4. 제출
5. 잘못된 결과 화면 확인

발표 멘트:

> 화면은 실패했습니다. 하지만 아직 프론트 문제인지 백엔드 문제인지 말하지 않겠습니다.

DevTools Network에서 `POST /quiz/submit`의 `200`만 짧게 보여준다. 응답 body를 발표자가 먼저 분석하지 않는다.

### 화면 6: AI 터미널/앱, 조사 요청

아래 프롬프트를 그대로 사용한다.

```text
Chrome DevTools MCP를 사용해 로그인부터 퀴즈 결과 확인까지 실행하세요.

목표:
- 제출 후 결과 화면이 잘못 표시되는 문제를 재현합니다.
- 아직 코드를 수정하지 않습니다.

조사 기준:
1. 사용자 조작 순서를 기록하세요.
2. Console 오류와 POST /quiz/submit Network 요청을 확인하세요.
3. 실제 request body, status, response body를 요약하세요.
4. 확인된 사실과 추정을 구분하세요.
5. 관련 프론트 파일과 백엔드 파일을 file:line 근거로 연결하세요.
6. 가능한 수정안과 각 수정안의 API 호환성 영향을 비교하세요.

금지:
- 운영 환경 접근
- 실제 사용자 데이터 사용
- DB 변경
- 원인 확인 전 코드 수정
```

AI가 브라우저를 조작하는 모든 과정을 설명하지 않는다. 다음 세 결과만 화면에서 강조한다.

1. 재현 성공 여부
2. Network의 실제 응답 필드
3. 프론트가 읽는 필드와 백엔드가 정의한 필드

AI 응답이 길면 다음 후속 요청으로 압축한다.

```text
조사 결과를 아래 다섯 줄로 다시 정리하세요.

1. 재현 결과
2. Network 근거
3. 프론트 코드 근거
4. 백엔드 코드 근거
5. 권장 수정안
```

### 화면 7: VS Code, 코드 근거

보여줄 파일은 최대 세 개로 제한한다.

1. 프론트 API 호출 또는 응답 처리 파일
2. FastAPI 응답 Schema
3. 필요한 경우 결과 화면 파일

발표 멘트:

> AI의 설명을 믿는 것이 아니라, 방금 본 Network 응답과 이 코드가 실제로 일치하는지 확인하겠습니다.

관련 없는 파일과 전체 diff는 보여주지 않는다.

### 화면 8: AI 터미널/앱, 수정 승인

```text
프론트와 백엔드의 현재 API 계약을 기준으로 권장 수정안을 적용하세요.

- 관련 없는 리팩터링은 하지 마세요.
- 변경 파일과 이유를 먼저 말하세요.
- lint와 관련 테스트를 실행하세요.
- 마지막에는 동일한 브라우저 흐름으로 다시 검증하세요.
```

AI가 제안한 변경 파일이 예상 범위를 벗어나면 중단하고 범위를 다시 제한한다.

### 화면 9: 테스트용 Chrome, 재검증

같은 순서로 다시 제출한다.

화면에서 확인할 것:

- `POST /quiz/submit` 응답
- 결과 점수
- 오답 목록
- Console 오류 없음

발표 멘트:

> 완료 기준은 AI가 "수정했습니다"라고 말한 것이 아닙니다. 같은 사용자 흐름에서 화면과 API가 함께 정상 동작하는 것입니다.

### 화면 10: Notion, 결론

Before와 After를 보여준다.

마지막 멘트:

> 프론트와 백엔드에 공통으로 필요한 AI 활용은 코드 생성이 아니라 실행 증거 연결입니다. 작은 사용자 흐름 하나부터 시작해 재현 시간과 원인 확인 시간을 측정하면 됩니다.

---

## 7. 라이브 실패 대응

| 실패 상황 | 즉시 전환할 대안 |
| --- | --- |
| MCP가 브라우저에 연결되지 않음 | DevTools에서 저장한 HAR와 Console 로그를 AI에게 파일로 제공 |
| 로그인 실패 | 미리 로그인한 테스트 프로필 또는 저장된 테스트 세션 사용 |
| 프론트/백엔드 서버 종료 | 사전 녹화한 재현 영상과 저장된 Network 캡처 사용 |
| AI 조사가 2분 이상 지연 | 사전에 저장한 조사 결과 Markdown을 열고 근거만 검증 |
| AI가 엉뚱한 파일 수정 | 변경을 승인하지 않고 준비한 정답 diff를 설명 |
| 수정 후 검증 실패 | 실패를 숨기지 말고 미리 준비한 정상 브랜치로 동일 흐름 시연 |
| YouTube 버퍼링 | 영상 생략 후 공식 사례 스크린샷 한 장 사용 |

백업 자료는 아래 네 개만 준비하면 된다.

```text
backup/01-reproduction.mp4
backup/02-network-response.png
backup/03-investigation-result.md
backup/04-fixed-flow.mp4
```

백업 자료는 발표 저장소에 실제 운영 정보 없이 만든다.

---

## 8. 데모에서 하지 않을 것

- Claude와 Codex 결과 비교
- 새 MCP 서버 직접 개발
- GitHub 이슈 또는 PR 생성
- 운영 로그와 운영 계정 연결
- 여러 버그를 동시에 수정
- 테스트 코드를 길게 설명
- AI 응답 전체를 처음부터 끝까지 읽기

이 발표의 실습 범위는 한 문장이다.

> 브라우저에서 한 문제를 재현하고, Network와 양쪽 코드를 연결해 원인을 찾은 뒤, 같은 흐름으로 수정 결과를 확인한다.

---

## 9. 발표 후 작업 순서

1. 발표용 fault injection을 구현한다.
2. Chrome DevTools MCP를 Claude 또는 Codex 한쪽에 연결한다.
3. 조사 프롬프트를 실제로 실행해 출력 길이를 조정한다.
4. 10분 이내로 데모가 끝나는지 리허설한다.
5. 재현 영상, HAR, 조사 결과, 수정 후 영상을 백업한다.
6. Notion 문구를 실제 데모 결과에 맞춰 최종 수정한다.
